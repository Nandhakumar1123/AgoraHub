# API base URL - backend must be running on this address
# Web: use localhost. Mobile: use your computer's LAN IP (e.g. http://192.168.1.100:3002/api)
# Backend uses port 3002 by default (see Backend/.env PORT)
EXPO_PUBLIC_API_URL=http://localhost:3002/api
